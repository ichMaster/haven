# Version v0 (phase v0.2) — Execution Report

**Date:** 2026-06-08
**Branch:** main
**Label:** v0::version:0 (phase v0.2 issues, HVN-012…015)
**Target version:** 0.2.0 (phase v0.2 — not yet released; see Version note)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All 4 issues of phase **v0.2** (In-character chat, frontend-only) implemented, validated, committed, pushed, and closed in dependency order. The chat was added into the existing single file [lili_house_aitown.jsx](../../../lili_house_aitown.jsx); still **no backend** (the browser calls Claude directly — the only phase the key lives client-side).

**Final validation:** `npm test` → **94/94 passing** across 15 test files (24 of them v0.2: chat-prompt 5, chat-client 6, chat-ui 3, chat-fallback 10); `npm run build` (Vite production) succeeds.

## Issues

| # | HVN ID | Title | Phase | Status | Commit | Files | Tests |
|---|--------|-------|-------|--------|--------|-------|-------|
| 12 | HVN-012 | Лілі's canon & system-prompt assembly (`buildSystemPrompt`) | v0.2 | completed | 1f8adff | 2 | pass (5) |
| 13 | HVN-013 | Frontend Claude chat client (`askLili`) | v0.2 | completed | 0feb182 | 7 | pass (6) |
| 14 | HVN-014 | Chat panel UI (input + transcript) | v0.2 | completed | 8e70f89 | 2 | pass (3) |
| 15 | HVN-015 | Graceful API-error fallback & voice shaping | v0.2 | completed | 2550733 | 2 | pass (10) |

Dependency order honored: #12 → #13 → #14 → #15.

## Detailed Results

### HVN-012: Лілі's canon & system-prompt assembly
**Status:** completed · **Commit:** 1f8adff · **Files:** `lili_house_aitown.jsx`, `tests/chat-prompt.test.js`
**Validation:** 5/5 — `LILI_CANON` voice rules; `buildSystemPrompt` apart (studio/office) vs together with `ROOMS` names; `liveContext` from sim (reuses `roomAt`/`roomView`, no duplicated room math).

### HVN-013: Frontend Claude chat client (`askLili`)
**Status:** completed · **Commit:** 0feb182 · **Files:** `lili_house_aitown.jsx`, `tests/chat-client.test.js`, `.env.example`, `vite.config.js`, `stubs/empty.mjs`, `package.json`, `package-lock.json`
**Validation:** 6/6 — model `claude-opus-4-8` + small `max_tokens`, assembled system + history + user turn (no prefill), text-block extraction; injected fake client (no network). Browser-build fix: client-only SDK entry + alias of the SDK's Node-only `agent-toolset` to a stub. `npm run build` green.

### HVN-014: Chat panel UI
**Status:** completed · **Commit:** 8e70f89 · **Files:** `lili_house_aitown.jsx`, `tests/chat-ui.test.jsx`
**Validation:** 3/3 (mounted) — panel/input/send render; send → user turn + grounded reply (context from `simRef.current`) + input cleared; movement keys ignored while the chat input is focused (and still work otherwise). Visual polish deferred to manual DoD.

### HVN-015: Graceful API-error fallback & voice shaping
**Status:** completed · **Commit:** 2550733 · **Files:** `lili_house_aitown.jsx`, `tests/chat-fallback.test.js`
**Validation:** 10/10 — `shapeReply` (trim/collapse, bullets, enumerators), `pickFallback` by error kind (typed `AuthenticationError`/`PermissionDeniedError` + 401/403 → offline; transient → distracted), `safeAskLili` success/list-flatten/transient/auth/missing-key/empty paths. Offline note rendered when no key.

## Phase v0.2 Definition of Done

> You can hold a conversation with Лілі that reflects where she is and what she is doing.

- **Met (host-tested):** the system prompt is assembled from canon + live sim context (her room/action, your room, together); the chat client sends it and returns her short reply; the panel sends grounded messages and renders the transcript; failures and a missing key never surface raw errors.
- **Manual confirmation (deferred per skill):** the live conversational *feel* with a real `VITE_ANTHROPIC_API_KEY` — run `npm run dev`, set the key in `.env`, and talk to Лілі while she moves between rooms. All host-testable logic (prompt assembly, client request shape, UI send/guard, fallback/shaping) is unit-verified with an injected fake client (no network in CI).

## Notes

- **Key handling:** v0.2 is the only phase with the Anthropic key in the frontend (a gitignored `VITE_ANTHROPIC_API_KEY`, documented in `.env.example`). From **v1.5** chat moves to `POST /chat` and the key is server-side only.
- **Build note:** the Anthropic SDK's package root re-exports a Node-only `agent-toolset`; the prototype imports `@anthropic-ai/sdk/client` and aliases that toolset to `stubs/empty.mjs` in `vite.config.js` so the browser bundle builds.
- **Version:** NOT bumped (per skill policy — no automatic version bump). When ready, `/release-version v0.2` cuts `0.2.0` (this completes the whole **v0** prototype: v0.1 + v0.2).

## Next Steps

- v0 (the graphical prototype) is now functionally complete: v0.1 (autonomous Лілі + world) **and** v0.2 (in-character chat). Recommended: a manual `npm run dev` pass with a real key, then `/release-version v0.2` (or `v0.1` first if you want a staged tag).
- v1 (living world & terminal/telnet server) is the next ROADMAP version — author `v1-issues.md` and `/upload-issues` when ready. v1 ports the world to a headless Python backend and exposes it over a telnet command server (key server-side); the REST API + graphical thin client follow in v2.
