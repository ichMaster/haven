"""Haven backend (v1+).

The headless world and simulation (v1.1), plus the telnet command server (v1.2+)
and the REST API (v2+). The backend owns the world and ticks it; the API key is
held server-side only. See specification/ARCHITECTURE.md and
specification/TERMINAL_SERVER_SPEC.md.
"""

__version__ = "0.1.0"
